{
  "name": "ellipsoid",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:atfzl/ellipsoid.git",
  "author": "Atif Afzal <atif5801@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "concurrently --raw --kill-others 'cd server && yarn start' 'cd client && yarn start' 'cd showcase && yarn start'",
    "postinstall": "concurrently --raw 'cd server && yarn' 'cd client && yarn' 'cd showcase && yarn'"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
